{
  "name": "fffonline",
  "version": "1.0.0",
  "description": "Farm Fight Formula Online server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "rollup -c"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "rollup": "^4.0.0"
  }
}
